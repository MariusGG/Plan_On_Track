import React from 'react';
import { shallow } from 'enzyme';
import SessionListings from '../src/components/SessionListings.js'

describe( 'SessionListings', () => {
  function testID(id) {
    return cmp => cmp.props().testID === id;
  }
  it('Displays the lists of sessions', () => {

  });
});
